services:
  portfolio:
    build:
      context: .
      dockerfile: dockerfile
    container_name: henry-yusuf-portfolio
    restart: unless-stopped
    network_mode: bridge
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
